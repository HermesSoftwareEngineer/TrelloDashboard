{
  "buildCommand": "cd trellodashboard && npm install && npm run build",
  "outputDirectory": "trellodashboard/dist",
  "installCommand": "cd trellodashboard && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
